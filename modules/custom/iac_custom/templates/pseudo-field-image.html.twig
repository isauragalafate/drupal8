{#
/**
 * @file
 * Theme override to display the IAC pseudo field image.
 *
 * Available variables:
 * - image: Associative rray with the properties (src, alt) of the image.
 */
#}

{% if image %}
    <img src="{{ image.src }}" alt="{{ image.alt }}">
{% endif %}